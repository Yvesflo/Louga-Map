# TODO

- [x] Add distance bands above 10 km (10–20, 20–50, 50–100 km) in `map.js`.
- [x] Increase default max distance filter to 100 km (update slider + initial filter state).
- [x] Update UI text in `index.html`/print header to show 100 km instead of 10 km.
- [x] Update “uncovered villages” popup text to reflect 100 km instead of 10 km.
- [ ] Run the app locally (Live Server) and verify:
  - Legend shows the new bands.
  - Slider allows values up to 100 km.
  - Stats/chart update with the new bands.
  - Covered/uncovered logic still behaves correctly.
  - CSV export and popups are consistent with the new max distance.

